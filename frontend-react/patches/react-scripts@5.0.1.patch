diff --git a/config/webpack.config.js b/config/webpack.config.js
index e465d8e7a00ec889e9c4c29ba7812019f8319a41..7809717a4218aff6abcd4e4bef33626919de9b30 100644
--- a/config/webpack.config.js
+++ b/config/webpack.config.js
@@ -37,6 +37,7 @@ const ReactRefreshWebpackPlugin = require('@pmmmwh/react-refresh-webpack-plugin'
 const getCacheIdentifier = require('react-dev-utils/getCacheIdentifier');
 // @remove-on-eject-end
 const createEnvironmentHash = require('./webpack/persistentCache/createEnvironmentHash');
+const FaviconsWebpackPlugin = require('favicons-webpack-plugin');
 
 // Source maps are resource heavy and can cause out of memory issue for large source files.
 const shouldUseSourceMap = process.env.GENERATE_SOURCEMAP !== 'false';
@@ -788,6 +789,30 @@ module.exports = function (webpackEnv) {
             },
           },
         }),
+      isEnvProduction && new FaviconsWebpackPlugin({
+        logo: './logo.svg',
+        prefix: '',
+        mode: 'webapp',
+        favicons: {
+          appName: "Musik",
+          appShortName: "Musik",
+          background: "#636363",
+          theme_color: "#ffffff",
+          display: "standalone",
+          orientation: "any",
+          scope: "/",
+          start_url: "/",
+          pixel_art: false,
+          icons: {
+            android: true,
+            appleIcon: { offset: 8 },
+            appleStartup: true,
+            favicons: true,
+            windows: { offset: 16 },
+            yandex: true
+          }
+        }
+      }),
     ].filter(Boolean),
     // Turn off performance processing because we utilize
     // our own hints via the FileSizeReporter
