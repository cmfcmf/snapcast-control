diff --git a/config/webpack.config.js b/config/webpack.config.js
index e465d8e7a00ec889e9c4c29ba7812019f8319a41..f9f01adebb5c157ea75896036f68959eb0509a78 100644
--- a/config/webpack.config.js
+++ b/config/webpack.config.js
@@ -38,6 +38,8 @@ const getCacheIdentifier = require('react-dev-utils/getCacheIdentifier');
 // @remove-on-eject-end
 const createEnvironmentHash = require('./webpack/persistentCache/createEnvironmentHash');
 
+const FaviconsWebpackPlugin = require('favicons-webpack-plugin');
+
 // Source maps are resource heavy and can cause out of memory issue for large source files.
 const shouldUseSourceMap = process.env.GENERATE_SOURCEMAP !== 'false';
 
@@ -788,6 +790,30 @@ module.exports = function (webpackEnv) {
             },
           },
         }),
+      isEnvProduction && new FaviconsWebpackPlugin({
+        logo: './logo.svg',
+        prefix: '',
+        mode: 'webapp',
+        favicons: {
+          appName: "Musik",
+          appShortName: "Musik",
+          background: "#636363",
+          theme_color: "#ffffff",
+          display: "standalone",
+          orientation: "any",
+          scope: "/",
+          start_url: "/",
+          pixel_art: false,
+          icons: {
+            android: true,
+            appleIcon: { offset: 8 },
+            appleStartup: true,
+            favicons: true,
+            windows: { offset: 16 },
+            yandex: true
+          }
+        }
+      }),
     ].filter(Boolean),
     // Turn off performance processing because we utilize
     // our own hints via the FileSizeReporter
diff --git a/config/webpackDevServer.config.js b/config/webpackDevServer.config.js
index 522a81b9b2b2723822513390a9d7e0fc38f75066..38bcef59d09a3044c1243c15c64cf289013c5cfb 100644
--- a/config/webpackDevServer.config.js
+++ b/config/webpackDevServer.config.js
@@ -43,7 +43,7 @@ module.exports = function (proxy, allowedHost) {
     // really know what you're doing with a special environment variable.
     // Note: ["localhost", ".localhost"] will support subdomains - but we might
     // want to allow setting the allowedHosts manually for more complex setups
-    allowedHosts: disableFirewall ? 'all' : [allowedHost],
+    allowedHosts: disableFirewall ? 'all' : ["localhost", ".localhost"],
     headers: {
       'Access-Control-Allow-Origin': '*',
       'Access-Control-Allow-Methods': '*',
